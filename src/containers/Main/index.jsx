import React from 'react';
import {ProductList} from '../../components';

class Main extends React.Component {
    render() {
        return (
            <main>
                <ProductList/>
            </main>
        );
    }
}

export default Main;
